{
	"name": "quaesitor-cli",
	"version": "1.0.10",
	"description": "a command–line interface for quaesitor",
	"main": "dist/index.js",
	"bin": {
		"quaesitor": "dist/index.js"
	},
	"scripts": {
		"start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
		"create": "npm run build && npm run test",
		"build": "tsc -p .",
		"test": "sudo npm i -g && quaesitor",
		"refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/dpl10/quaesitor-cli.git"
	},
	"author": "Damon P. Little",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/dpl10/quaesitor-cli/issues"
	},
	"homepage": "https://github.com/dpl10/quaesitor-cli#readme",
	"devDependencies": {
		"@types/node": "^14.6.3",
		"nodemon": "^2.0.4",
		"ts-node": "^9.0.0",
		"typescript": "^3.9.7"
	},
	"dependencies": {
		"@tensorflow/tfjs-node": "^2.3.0",
		"mem": "^6.1.1",
		"quaesitor": "^1.0.10"
	}
}
